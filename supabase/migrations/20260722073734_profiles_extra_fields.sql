-- ============================================================================
-- Доп. поля профиля: дата рождения, соцсети (всё опционально).
-- ============================================================================
-- RLS не меняем — profiles_update_self уже разрешает пользователю обновлять
-- любые свои колонки, кроме role/status (см. 20260718_auth_owner_dashboard.sql),
-- новые колонки автоматически попадают под это же правило.
-- ============================================================================

begin;

alter table profiles
  add column birth_date date,
  add column instagram text,
  add column whatsapp text,
  add column telegram text;

commit;
