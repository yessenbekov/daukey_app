-- ============================================================================
-- Публичная страница "Лошади клуба" теперь показывает весь ростер клуба,
-- включая лошадей, закреплённых за конкретными владельцами (не только те,
-- что выставлены на продажу). Ослабляем select-политику horses: раньше
-- анонимный посетитель видел только is_available = true.
-- ============================================================================

begin;

drop policy if exists horses_select on horses;

create policy horses_select on horses
  for select
  using (true);

commit;
